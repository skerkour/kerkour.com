# Going full speed with async

## Summary

Unfortunately, when a program spends most of its time in I/O operations, multi-threading is not a panacea. We will learn how async makes Rust code really, really fast and refactor our scanner to async code.




enjeux:
- async rust
- introduire d'autres types de scan
- cli (clap)

hook:

plan:


notre code n'etait pas parfait, pas configurable, et pas assez rapide
montrer a la fois comment utiliser des APIs async
et des APIs

preemtive scheduling vs cooperative

ajouter des modules, mai seulement a la rache
